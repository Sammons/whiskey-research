[package]
name = "whiskey-simulator"
version = "0.1.0"
edition = "2021"

[dependencies]
nalgebra = "0.32"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
opt-level = 3
lto = true
